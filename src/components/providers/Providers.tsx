"use client";

// TODO: Initialize PostHog and any other client-side providers here.
// Wrap children and add this component to app/layout.tsx.

export default function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
